package com.example.myfit.ui.theme

import androidx.compose.ui.graphics.Color

// ── Athletic Blue — primary ───────────────────────────────────────────────────
val Blue10  = Color(0xFF001848)
val Blue20  = Color(0xFF002D77)
val Blue30  = Color(0xFF0042A8)
val Blue40  = Color(0xFF1D83EA)   // primary, light theme
val Blue80  = Color(0xFFB4C5FF)   // primary, dark theme
val Blue90  = Color(0xFFDAE2FF)
val Blue95  = Color(0xFFEEF0FF)

// ── Teal — secondary ─────────────────────────────────────────────────────────
val Teal40  = Color(0xFF006B5E)
val Teal80  = Color(0xFF4FDBC8)
val Teal90  = Color(0xFFAFF5E6)

// ── Green — tertiary ─────────────────────────────────────────────────────────
val Green10 = Color(0xFF0E2900)   // on-container text (dark)
val Green20 = Color(0xFF1C4500)   // dark theme onTertiary
val Green30 = Color(0xFF2A5500)   // dark theme tertiaryContainer
val Green40 = Color(0xFF56BD23)   // vibrant green (light theme)
val Green80 = Color(0xFF9EF065)   // bright lime (dark theme)
val Green90 = Color(0xFFC7FEA0)   // light green container / user bubble

// ── Neutral ───────────────────────────────────────────────────────────────────
val NeutralBg   = Color(0xFFF5FAF5)  // light background (subtle green tint)
val NeutralDark = Color(0xFF111411)  // dark background  (subtle green tint)

// ── Surface containers (green-tinted, replaces M3 pinkish baseline) ───────────
val SurfaceContainerLowest      = Color(0xFFFBFFFB)
val SurfaceContainerLow         = Color(0xFFF2F7F2)
val SurfaceContainerMid         = Color(0xFFEEF3EE)
val SurfaceContainerHigh        = Color(0xFFE6ECE6)
val SurfaceContainerHighest     = Color(0xFFE0E6E0)
val SurfaceContainerLowestDark  = Color(0xFF0B110B)
val SurfaceContainerLowDark     = Color(0xFF111711)
val SurfaceContainerMidDark     = Color(0xFF161C16)
val SurfaceContainerHighDark    = Color(0xFF1B221B)
val SurfaceContainerHighestDark = Color(0xFF202820)

// ── Semantic macro colors ─────────────────────────────────────────────────────
// Accent (progress bars, icons)
val ProteinAccent = Color(0xFFE8956D)  // Белки — персиковый
val FatAccent     = Color(0xFFD4AC2A)  // Жиры  — янтарный
val CarbsAccent   = Color(0xFF52B788)  // Углеводы — мятный
val WaterAccent   = Color(0xFF4BAED8)  // Вода  — небесно-голубой
// Container (card backgrounds)
val ProteinBg = Color(0xFFFED6BC)      // Белки — пастельный персик
val FatBg     = Color(0xFFFFFADD)      // Жиры  — пастельный крем
val CarbsBg   = Color(0xFFC3FBD8)      // Углеводы — пастельная мята
val WaterBg   = Color(0xFFDEF7FE)      // Вода  — пастельный голубой
val MacroText = Color(0xFF2A323D)      // текст поверх пастельных карточек
